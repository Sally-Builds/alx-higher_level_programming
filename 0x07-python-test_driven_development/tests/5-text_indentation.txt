"""
Split the input text with 2 new lines after each '.', '?', or ':' character.

Args:
    text (str): The input text.

Raises:
    TypeError: If 'text' is not a string.

Examples:
    # Basic example
    >>> text_indentation = __import__('5-text_indentation').text_indentation
    >>> text = "This is a test. It has multiple sentences. What do you think: Does it work?"
    >>> text_indentation(text)
    This is a test.

    It has multiple sentences.

    What do you think:

    Does it work?

    # Edge case: Empty input
    >>> text_indentation("")
    ""

    # Edge case: Input with no '.', '?', or ':' characters
    >>> text_indentation("No special characters here")
    No special characters here

    # Edge case: Input with only spaces
    >>> text_indentation("    ")
    ""

    # Edge case: Input with spaces and special characters
    >>> text_indentation("This. . .  has spaces : and ? ? ?")
    This.

    It has spaces :

    and ?

    # Edge case: Input with multiple spaces between characters
    >>> text_indentation("Spacing...  is weird!   Right? ")
    Spacing...

    is weird!

    Right?

    # Edge case: Input with only '.', '?', and ':' characters
    >>> text_indentation("...?..:..")
    ...

    ...

    ..
"""

